# @iconify-react/material-symbols-light

⚠️ This is an alternative repository. Main repository has moved to [GitLab](https://gitlab.com/iconify-icons/react/-/tree/material-symbols-light).
**Material Symbols Light**

Author: [Google](https://github.com/google/material-design-icons)

License: [Apache 2.0](https://github.com/google/material-design-icons/blob/master/LICENSE)

Browse all icons: [preview Material Symbols Light on Iconify](https://icon-sets.iconify.design/material-symbols-light/)

## Installation

```bash
npm install gitlab:iconify-icons/react#material-symbols-light
```

or
```bash
npm install github:iconify/icons-react#material-symbols-light
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/material-symbols-light/downloading';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/material-symbols-light/privacy-tip';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/material-symbols-light/filter-drama-outline';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
