# @iconify-react/circum

Icon set prefix: **circum**

Icon set name: **Circum Icons**

Author: **Klarr Agency**

License: **[Mozilla Public License 2.0](https://github.com/Klarr-Agency/Circum-Icons/blob/main/LICENSE)**

Repository: **https://github.com/Klarr-Agency/Circum-Icons**

Browse all icons: **[Preview Circum Icons](https://icon-sets.iconify.design/circum/)**

## Installation

```bash
npm install github:iconify/icons-react#circum
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/circum/text';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/circum/pill';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/circum/zoom-out';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
