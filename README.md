# @iconify-react/token

**Web3 Icons**

Author: [0xa3k5](https://github.com/0xa3k5/web3icons)

License: [MIT](https://github.com/0xa3k5/web3icons/blob/main/LICENCE)

Browse all icons: [preview Web3 Icons on Iconify](https://icon-sets.iconify.design/token/)

## Installation

```bash
npm install github:iconify/icons-react#token
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/token/bit';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/token/dog';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/token/eth';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
