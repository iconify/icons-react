# @iconify-react/formkit

Icon set prefix: **formkit**

Icon set name: **FormKit Icons**

Author: **FormKit, Inc**

License: **[MIT](https://github.com/formkit/formkit/blob/master/packages/icons/LICENSE)**

Repository: **https://github.com/formkit/formkit/tree/master/packages/icons**

Browse all icons: **[Preview FormKit Icons](https://icon-sets.iconify.design/formkit/)**

## Installation

```bash
npm install github:iconify/icons-react#formkit
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/formkit/checkbox';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/formkit/reorder';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/formkit/submit';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
