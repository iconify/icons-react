# @iconify-react/mono-icons

⚠️ This icon set is deprecated and no longer maintained. Use it at your own risk.


**Mono Icons**

Author: [Mono](https://github.com/mono-company/mono-icons)

License: [MIT](https://github.com/mono-company/mono-icons/blob/master/LICENSE.md)

Browse all icons: [preview Mono Icons on Iconify](https://icon-sets.iconify.design/mono-icons/)

## Installation

```bash
npm install github:iconify/icons-react#mono-icons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/mono-icons/user';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/mono-icons/log-in';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/mono-icons/play';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
