# @iconify-react/keyline-icons

⚠️ This is an alternative repository. Main repository has moved to [GitLab](https://gitlab.com/iconify-icons/react/-/tree/keyline-icons).
**Keyline Icons**

Author: [Keyline Icons](https://github.com/keyline-icons/keyline-icons)

License: [MIT](https://github.com/keyline-icons/keyline-icons/blob/main/LICENSE)

Browse all icons: [preview Keyline Icons on Iconify](https://icon-sets.iconify.design/keyline-icons/)

## Installation

```bash
npm install gitlab:iconify-icons/react#keyline-icons
```

or
```bash
npm install github:iconify/icons-react#keyline-icons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/keyline-icons/arrow-up-narrow-wide-duotone';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/keyline-icons/credit-card-fill';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/keyline-icons/upload';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
