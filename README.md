# @iconify-react/uit

**Unicons Thin Line**

Author: [Iconscout](https://github.com/Iconscout/unicons)

License: [Apache 2.0](https://github.com/Iconscout/unicons/blob/master/LICENSE)

Browse all icons: [preview Unicons Thin Line on Iconify](https://icon-sets.iconify.design/uit/)

## Installation

```bash
npm install github:iconify/icons-react#uit
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/uit/circuit';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/uit/favorite';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/uit/toggle-on';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
