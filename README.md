# @iconify-react/geo

Icon set prefix: **geo**

Icon set name: **GeoGlyphs**

Author: **Sam Matthews**

License: **[MIT](https://github.com/cugos/geoglyphs/blob/main/LICENSE.md)**

Repository: **https://github.com/cugos/geoglyphs**

Browse all icons: **[Preview GeoGlyphs](https://icon-sets.iconify.design/geo/)**

## Installation

```bash
npm install github:iconify/icons-react#geo
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/geo/turf-center';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/geo/turf-erased';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/geo/turf-point-on-line';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
