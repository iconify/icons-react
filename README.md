# @iconify-react/ph

**Phosphor**

Author: [Phosphor Icons](https://github.com/phosphor-icons/core)

License: [MIT](https://github.com/phosphor-icons/core/blob/main/LICENSE)

Browse all icons: [preview Phosphor on Iconify](https://icon-sets.iconify.design/ph/)

## Installation

```bash
npm install github:iconify/icons-react#ph
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/ph/folder-open-duotone';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/ph/check-square-offset-thin';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/ph/pencil-line-fill';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
