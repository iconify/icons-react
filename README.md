# @iconify-react/dinkie-icons

Icon set prefix: **dinkie-icons**

Icon set name: **Dinkie Icons**

Author: **atelierAnchor**

License: **[MIT](https://github.com/atelier-anchor/dinkie-icons/blob/main/LICENSE)**

Repository: **https://github.com/atelier-anchor/dinkie-icons**

Browse all icons: **[Preview Dinkie Icons](https://icon-sets.iconify.design/dinkie-icons/)**

## Installation

```bash
npm install github:iconify/icons-react#dinkie-icons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/dinkie-icons/lock-small-filled';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/dinkie-icons/u1faab-small';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/dinkie-icons/battery-small';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
