# @iconify-react/bytesize

Icon set prefix: **bytesize**

Icon set name: **Bytesize Icons**

Author: **Dan Klammer**

License: **[MIT](https://github.com/danklammer/bytesize-icons/blob/master/LICENSE.md)**

Repository: **https://github.com/danklammer/bytesize-icons**

Browse all icons: **[Preview Bytesize Icons](https://icon-sets.iconify.design/bytesize/)**

## Installation

```bash
npm install github:iconify/icons-react#bytesize
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/bytesize/desktop';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/bytesize/code';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/bytesize/sign-out';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
