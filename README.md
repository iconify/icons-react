# @iconify-react/file-icons

Icon set prefix: **file-icons**

Icon set name: **File Icons**

Author: **John Gardner**

License: **[ISC](https://github.com/file-icons/icons/blob/master/LICENSE.md)**

Repository: **https://github.com/file-icons/icons**

Browse all icons: **[Preview File Icons](https://icon-sets.iconify.design/file-icons/)**

## Installation

```bash
npm install github:iconify/icons-react#file-icons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/react/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-react/file-icons/adobe';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-react/file-icons/chartjs';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-react/file-icons/dom';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
